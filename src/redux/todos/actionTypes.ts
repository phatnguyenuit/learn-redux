// action type for each reducer should be unique
const actionTypes = {
  request: 'todos/request',
  requestSuccess: 'todos/requestSuccess',
  requestFail: 'todos/requestFail',
};

export default actionTypes;
